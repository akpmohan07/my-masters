foo:
  push  %ebp
  mov   %esp,%ebp
  sub   $0x10,%esp
  movl  $0, -0x4(%ebp)         # acc = 0
  jmp   .Lcond
.Lbody:
  mov   0x8(%ebp), %eax        # a
  add   %eax, -0x4(%ebp)       # acc += a
  subl  $1, 0xC(%ebp)          # b--
.Lcond:
  cmpl  $0, 0xC(%ebp)          # if (b > 0) goto body
  jg    .Lbody
  mov   -0x4(%ebp), %eax
  leave
  ret

  a=0;